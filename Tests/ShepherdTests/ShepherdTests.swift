import Quick
import Nimble
@testable import Shepherd

final class ShepherdTests: QuickSpec {

    override func spec() {
        describe("Shepherd tests") {
            it("should run") {
                expect(true).to(beTrue())
            }
        }
    }

}
